import React, { useState, useEffect } from 'react'; import { useI18n } from '../lib/i18n'; import { SKILLS } from '../lib/catalog'; const UIVERSE_TOGGLE_STYLES = ` /* From Uiverse.io by njesenberger */ .toggle-container { --knob-size: 1.75em; display: flex; justify-content: center; position: relative; } .toggle-input { position: absolute; z-index: 2; bottom: 132.5%; border-radius: 50%; transform: rotate(-25deg); transform-origin: 50% 4.75em; width: var(--knob-size); height: var(--knob-size); opacity: 0; font: inherit; transition: transform .24s cubic-bezier(.65, 1.35, .5, 1); cursor: pointer; } .toggle-input:checked { transform: rotate(25deg); } .toggle-handle-wrapper { position: absolute; z-index: 1; bottom: -135%; -webkit-mask-image: linear-gradient(to bottom, #000 62.125%, transparent 50%); mask-image: linear-gradient(to bottom, #000 62.125%, transparent 50%); width: 200%; overflow: hidden; } .toggle-handle { display: flex; flex-direction: column; align-items: center; transform: rotate(-25deg); transform-origin: bottom center; transition: transform .24s cubic-bezier(.65, 1.35, .5, 1); } .toggle-input:checked + .toggle-handle-wrapper > .toggle-handle { transform: rotate(25deg); } .toggle-handle-knob { position: relative; z-index: 1; border-radius: 50%; width: var(--knob-size); height: var(--knob-size); background-image: radial-gradient(farthest-corner at 70% 30%, #fedee2 4%, #d63534 12% 24%, #a81a1a 50% 65%, #d63534 75%); transition: transform .24s cubic-bezier(.65, 1.35, .5, 1); } .toggle-input:checked + .toggle-handle-wrapper .toggle-handle-knob { transform: rotate(-90deg); } .toggle-handle-knob::after { content: ''; position: absolute; top: 0; left: 0; border-radius: inherit; width: 100%; height: 100%; box-shadow: inset 0 0 8px 2px rgb(255 255 255 / .4); opacity: 0; transition: opacity .2s; } @media (hover: hover) { .toggle-input:hover + .toggle-handle-wrapper .toggle-handle-knob::after, .toggle-input:focus-visible + .toggle-handle-wrapper .toggle-handle-knob::after { opacity: 1; } } .toggle-handle-bar-wrapper { position: relative; width: .5em; height: 3em; } .toggle-handle-bar { position: absolute; top: calc(var(--knob-size) / 2 * -1); left: 0; width: 100%; height: calc(100% + var(--knob-size) / 2); background-image: linear-gradient(to right, #777475, #a4a4a4, #fff 45% 55%, #a4a4a4, #777475); background-position-x: .06125em; transition: background-position-x .24s cubic-bezier(.65, 1.35, .5, 1); box-shadow: inset 0 1em .25em rgb(0 0 0 / .4); } .toggle-input:checked + .toggle-handle-wrapper .toggle-handle-bar { background-position-x: -.06125em; } .toggle-base { position: relative; border-radius: 3.125em; padding: .25em; width: 3.5em; height: 1.125em; background-color: #fff; background-image: linear-gradient(to bottom, #fff, #d7d7d7); box-shadow: 0 -.25em .5em #fff, 0 .25em .5em #d7d7d7; } .toggle-base-inside { position: relative; border-radius: inherit; width: 100%; height: 100%; background-image: linear-gradient(to bottom, #a6a6a6, #7d7d7d); box-shadow: inset 0 .0625em rgb(255 255 255 / .2), inset 0 -.03125em rgb(255 255 255 / 1), inset 0 -.0625em .25em rgb(0 0 0 / .1); } .toggle-base-inside::after { content: ''; position: absolute; border-radius: inherit; width: 100%; height: 100%; background-image: linear-gradient(to bottom, #5ab054, #438c3c); box-shadow: inherit; opacity: 0; transition: opacity .24s cubic-bezier(.65, 1.35, .5, 1); } .toggle-input:checked ~ .toggle-base .toggle-base-inside::after { opacity: 1; } .skill-toggle-wrapper { display: flex; flex-direction: column; align-items: center; gap: 0.5rem; } .skill-label { font-size: 0.875rem; font-weight: 500; color: #6b7280; transition: color 0.2s ease; } .skill-label.skill-selected { color: #d63534; } .skills-toggle-container { padding: 0.5rem; } `; export default function SkillToggle({ selectedSkills, setSelectedSkills }) { const { t } = useI18n(); const [isSkillSelected, setIsSkillSelected] = useState(() => ({})); useEffect(() => { Object.keys(SKILLS).forEach(skill => { setIsSkillSelected(prev => ({ ...prev, [skill]: selectedSkills.includes(skill) })); }); }, [selectedSkills]); const canDisableSkill = (skill) => selectedSkills.length > 1 || selectedSkills.includes(skill); const toggleSkill = (skill) => { if (!canDisableSkill(skill)) return; setSelectedSkills(prev => prev.includes(skill) ? prev.filter(s => s !== skill) : [...prev, skill]); }; const handleKeyDown = (e) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); const checkbox = e.target; if (checkbox && checkbox.id) { const label = document.querySelector(`label[for="${checkbox.id}"]`); if (label) label.click(); } } }; useEffect(() => { const checkboxes = document.querySelectorAll('input[type="checkbox"]'); checkboxes.forEach(cb => cb.addEventListener('keydown', handleKeyDown)); return () => { checkboxes.forEach(cb => cb.removeEventListener('keydown', handleKeyDown)); }; }, []); return ( <div className="skills-toggle-container"> <p className="text-xs text-terracotta mb-2">{t('select_skills')}</p> <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3"> {SKILLS.map(skill => { const isSelected = isSkillSelected[skill.key]; return ( <div key={skill.key} className="skill-toggle-wrapper"> <div className="relative inline-flex items-center"> <span className="sr-only">{skill.label}</span> <div className="toggle-container"> <input type="checkbox" id={`skill-${skill.key}`} className="toggle-input visually-hidden" aria-labelledby={`skill-label-${skill.key}`} tabIndex={0} checked={isSelected} onChange={() => toggleSkill(skill.key)} /> <label htmlFor={`skill-${skill.key}`} className="toggle-base" aria-pressed={isSelected}> <span className="toggle-base-inside" aria-hidden="true"></span> </label> </div> </div> <span className={`skill-label ${isSelected ? 'skill-selected' : ''}`} id={`skill-label-${skill.key}`}> {skill.label} </span> </div> ); })} </div> <style>{UIVERSE_TOGGLE_STYLES}</style> </div> ); }